{
  "framework": "nextjs",
  "buildCommand": "npx prisma generate && npx next build",
  "installCommand": "npm install",
  "regions": ["syd1"],
  "env": {
    "DATABASE_URL": "postgresql://dummy:dummy@dummy.dummy.com:5432/dummy?sslmode=require"
  }
}
